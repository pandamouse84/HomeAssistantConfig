scene_homework_toggle:
  icon_template: mdi:content-cut
  value_template: "{{- is_state('switch.scene_homework', 'on') -}}"
  turn_on:
    service: switch.toggle
    target:
      entity_id: switch.scene_homework
  turn_off:
